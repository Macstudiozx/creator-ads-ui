# Hermes Project Context

You are working inside `creator-ads-ui`, a cloneable Next.js app for Creator Ads workflows.

Follow `AGENTS.md` first. Important invariants:

- Never expose `.env.engine` secrets or Meta tokens to the browser.
- Keep Web UI Supabase keys public-only (`NEXT_PUBLIC_*`).
- Run `npm run lint`, `npm run typecheck`, and `npm run build` before saying the repo is ready.
- For GitHub packaging, ensure `.gitignore` excludes env files, build artifacts, generated output, and uploaded media.
